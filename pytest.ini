[pytest]
addopts =
    --base-url="Your product under test Base URL - (e.g. https://staging.or.qa.or.uat.yourapp.com)"
    --browser chromium
    --browser firefox
    --browser webkit
    --video=retain-on-failure
    --screenshot=only-on-failure
    --tracing=on
    --slowmo=200
    --html=report/index.html --self-contained-html
    -n auto
    --dist=loadscope
    -vv
    --junitxml=report/pytest-junit.xml
    --cov=.
    --cov-report=xml
    

testpaths =
    tests
python_files =
    test_*.py
generate_report_on_test = 
    True
    