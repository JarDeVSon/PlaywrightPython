[pytest]
addopts =
    --browser chromium webkit firefox
    --html=reports/report.html
    --self-contained-html
    --video=off
    --screenshot=only-on-failure
    --tracing=off
    -n auto

testpaths =
    tests